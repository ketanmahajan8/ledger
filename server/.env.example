# DATABASE_URL=postgres://user:password@localhost:5432/ledger
# PORT=4000
# CLIENT_ORIGIN=http://localhost:5173
# # Any long random string. Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=replace_with_a_long_random_string


DATABASE_URL=postgres://postgres:YOUR_ACTUAL_POSTGRES_PASSWORD@localhost:5432/ledger
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=e91f... (a real random string, not this text)